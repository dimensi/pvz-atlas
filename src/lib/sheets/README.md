Google Sheets adapters will live here.

Rules:
- server-side only;
- validate rows before returning them to sync logic;
- never use row numbers as stable IDs;
- treat row numbers as temporary adapter metadata only.
